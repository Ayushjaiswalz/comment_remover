services:
  - type: web
    name: smart-comment-remover
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python remove_comments_smart.py --web
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16
